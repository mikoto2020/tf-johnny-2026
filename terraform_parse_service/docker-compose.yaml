version: '3.8'

services:
  terraform-parse:
    build: 
      context: .
      dockerfile: Dockerfile
    image: tripla-sre-assignment/tf-parse:latest
    container_name: tf-parse-service
    ports:
      - "5000:5000"
    restart: always
